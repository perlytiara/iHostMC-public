# Optional: overrides server/relay-public-token.txt (public relay uses same token for all users)
FRP_API_TOKEN=
FRP_ALLOWED_HOST=play.ihost.one
FRP_API_ADDR=:8081
FRP_PORT_MIN=20000
FRP_PORT_MAX=60000
