# Dev defaults – committed so every dev (and CI) gets Account/billing without creating .env.
# Used when running: npm run dev, npm run tauri dev.
# Override with .env or .env.local if you use a different backend.

VITE_API_BASE_URL=https://api.ihost.one
VITE_WEBSITE_URL=https://ihost.one
