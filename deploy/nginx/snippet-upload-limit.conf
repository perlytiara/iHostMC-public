# Optional: include from http { } in /etc/nginx/nginx.conf to allow large sync/backup uploads globally.
# Add: include /opt/iHostMC/deploy/nginx/snippet-upload-limit.conf;
client_max_body_size 1024M;
