/**
 * Default relay token. Do not commit a real value.
 * Set VITE_RELAY_PUBLIC_TOKEN in .env or GitHub Secrets for builds.
 * Local dev: run `npm run sync-relay-token` after putting token in server/relay-public-token.txt (gitignored).
 */
export const RELAY_PUBLIC_TOKEN = "";
